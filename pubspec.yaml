name: tsdm_converter
description: "Converter among different dataframes for tsdm"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  flutter: ">=3.27.0 <4.0.0"
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  # Blocked by flutter
  collection: any
  equatable: ^2.0.7
  flex_color_scheme: ^8.0.2
  flutter:
    sdk: flutter
  gitsumu: ^0.5.3

dev_dependencies:
  build_runner: ^2.4.13
  custom_lint: ^0.6.10
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  pyramid_lint: ^2.0.1
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
